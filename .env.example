# Copy to .env and fill in. .env is gitignored — never commit real secrets.
# Anthropic (server-side only; key must never reach frontend JS)
ANTHROPIC_API_KEY=

# Neo4j connection
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=
