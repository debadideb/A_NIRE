# placeholder so the empty frontend/ dir is tracked.
# The existing vanilla HTML/CSS/JS mockup goes here as index.html (Step 7).
# Do NOT regenerate it; only the <svg> graph block becomes Cytoscape.js later.
